spring:
  application:
    name: greeting-service
  cloud:
    config:
      enabled: false

greeting-service:
  greeting: "Pronto"
  default-value: "Mondo!"

management:
  endpoints:
    web:
      exposure:
        include:
        - '*'