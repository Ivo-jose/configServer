spring:
  application:
    name: greeting-service
  cloud:
    config:
      enabled: false

greeting-service:
  greeting: "Hello"
  default-value: "World!"

management:
  endpoints:
    web:
      exposure:
        include:
        - '*'